spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/wordsprint?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: is_deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.example.wordsprint: info

jwt:
  secret: ${JWT_SECRET:wordsprint-dev-only-secret-key-do-not-use-in-production-32ch}
  expire-hours: 24

cors:
  allowed-origins: http://localhost:5173,http://127.0.0.1:5173
